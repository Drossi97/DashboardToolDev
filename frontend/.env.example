# URL del backend API - Solo para desarrollo local con backend separado
PUBLIC_SERVER_URL=http://localhost:3000

# Nota: En producción con Docker unificado, no necesitas esta variable
# El frontend usa rutas relativas (mismo origen)
